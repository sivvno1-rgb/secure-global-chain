"""Secure Global Chain backend.

This package currently contains the **security core** (identity + audit kernel)
that every bounded context shares, per ``docs/design_handoff/SECURITY.md``.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
